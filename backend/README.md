# Backend

FastAPI backend for VoiceCanvas.

## Setup

```bash
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
```

## Run

```bash
.venv/Scripts/python -m uvicorn main:app --reload
```

The API will be available at `http://127.0.0.1:8000`.
